micronaut:
  config-client:
    enabled: true
  application:
    name: native-config-demo
netty:
  default:
    allocator:
      max-order: 3
eureka:
  client:
    registration:
      enabled: false

spring:
  cloud:
    config:
      format: YAML
      uri: http://config:8888
      enabled: true